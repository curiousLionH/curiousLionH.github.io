---
layout: post
title: "Starting This Blog"
date: 2026-04-14
categories: note
tags: [intro]
math: true
---

I'm starting this blog to document what I read, build, and think about during my research journey.

## What to Expect

- **Paper Reviews** — summaries and critiques of papers I read
- **Tech Posts** — implementations, experiments, and engineering notes
- **Study Notes** — concept breakdowns and learning records

## Math Works

Inline: $\mathcal{L}(\theta) = -\log p_\theta(x)$

Display:

$$
p_\theta(x) = \int p_\theta(x \mid z)\, p(z)\, dz
$$

## Code Works

```python
import torch
import torch.nn as nn

class MLP(nn.Module):
    def __init__(self, dims: list[int]):
        super().__init__()
        layers = []
        for i in range(len(dims) - 1):
            layers += [nn.Linear(dims[i], dims[i+1]), nn.ReLU()]
        self.net = nn.Sequential(*layers[:-1])  # drop last ReLU

    def forward(self, x: torch.Tensor) -> torch.Tensor:
        return self.net(x)
```

More posts coming soon.
