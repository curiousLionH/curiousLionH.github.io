---
layout: post
title: "블로그를 시작합니다"
date: 2026-04-14
categories: note
tags: [intro]
math: true
---

PhD 과정에서 공부하고 연구하는 내용들을 기록하기 위해 블로그를 시작합니다.

## 블로그 구성

논문 리뷰, 기술 포스트, 공부 노트 세 가지 카테고리로 운영할 예정입니다.

## 수식 테스트

인라인 수식: $E = mc^2$

디스플레이 수식:

$$
\mathcal{L}(\theta) = -\frac{1}{N} \sum_{i=1}^{N} \log p_\theta(x_i)
$$

## 코드 테스트

```python
import torch
import torch.nn as nn

class SimpleModel(nn.Module):
    def __init__(self, input_dim, hidden_dim, output_dim):
        super().__init__()
        self.net = nn.Sequential(
            nn.Linear(input_dim, hidden_dim),
            nn.ReLU(),
            nn.Linear(hidden_dim, output_dim)
        )

    def forward(self, x):
        return self.net(x)
```

앞으로 꾸준히 기록하겠습니다!
